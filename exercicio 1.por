programa {
  funcao inicio() {
    inteiro base, altura, soma
    escreva ("Digite a base do retangulo:")
    leia (base)
    escreva ("Digite a altura do retangulo:")
    leia (altura)
    soma = altura * base
    escreva (soma)
  }
}
