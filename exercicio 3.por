programa {
  funcao inicio() {
    real peso, altura, soma, soma2, soma3
    escreva ("Digite a altura da pessoa:")
    leia (peso)
    escreva ("Digite o peso da pessoa:")
    leia (altura)
    soma = altura * altura
    soma2 = peso / soma
    soma3 = soma2 * 100000
    escreva (soma3)
  }
}
